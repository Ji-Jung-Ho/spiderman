<?
    // 루트에서 서브페이지로 가는 경로
    $path = '../../../';
    // 멤버에서 메인으로 가는 경로
    $sub_path = '../../main/';
    // 서브페이지 : 멤버 경로
    $sub2_path = '../';
    echo "<script>";
    echo "let _path =  '" . $path . "';";
    echo "</script>";
?>